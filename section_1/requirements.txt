# Section 1 dependencies
# Python 3.8 required (blis/spacy constraint).
#
# Poppler (Windows): download from
#   https://github.com/oschwartz10612/poppler-windows/releases
#   and place in <project_root>/poppler/  (auto-detected by utils.py)

# ── Core (always required) ────────────────────────────────────────────────
streamlit>=1.28.0
opencv-python>=4.8.0
easyocr>=1.7.0
pdf2image>=1.16.3
numpy==1.24.4          # pinned: spacy 3.6.1 requires numpy<2 on Python 3.8
Pillow>=10.0.0
spacy==3.6.1           # pinned: spacy>=3.7 requires blis which needs numpy>=2 (Python 3.8 incompatible)
editdistance>=0.8.1
pandas>=2.0.0
plotly>=5.18.0
pyspellchecker>=0.8.0
pytest>=7.0.0

# ── Optional OCR engines ──────────────────────────────────────────────────
# TrOCR (best for handwriting — microsoft/trocr-large-handwritten, ~1.3 GB):
#   pip install transformers torch torchvision
#
# PaddleOCR (balanced):
#   pip install paddleocr paddlepaddle
